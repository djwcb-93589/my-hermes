[project]
name = "my-hermes"
version = "0.1.0"
description = "Hermes — modular agent framework with scheduled tasks"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.100",
    "openai>=1.0",
    "pyyaml>=6.0",
    "uvicorn>=0.20",
]
