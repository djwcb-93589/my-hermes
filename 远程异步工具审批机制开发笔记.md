# 远程异步工具审批机制开发笔记

远程 Agent 可以访问本机文件时，安全目标不能只依赖少量危险命令正则，而应保证每个会触达文件内容或执行任意程序的操作都先停在用户面前。审批还是一个跨消息、跨进程的状态问题：用户看到的操作、数据库保存的操作和最终执行的操作必须是同一份数据。

### 1. 危险命令名单无法覆盖普通删除和文件读取

#### 1.1 问题描述

旧流程只在 Terminal 命中少量危险正则时进入审批，File 的路径操作则没有审批阶段。`rm -rf directory` 会被识别，但 `rm 1.md`、`cat report.md`、`ls /d` 和 File 的 `read`、`list`、`write` 都可以直接执行。

这会造成同一类本机文件能力具有不同的控制语义：递归删除被视为高风险，普通删除和批量读取却绕过用户确认。Gateway 又不能使用服务器控制台的 `input()`，所以命中旧规则的操作只能被直接阻止，无法在飞书里继续原任务。

#### 1.2 原因分析

危险正则回答的是“这条命令是否像已知的高风险命令”，不是“这次操作是否访问或改变了本机数据”。Shell 还可以通过 Python、PowerShell、重定向和新安装的程序表达相同副作用，维护命令黑名单无法形成封闭边界。

旧时序如下：

```text
模型调用 Terminal
→ 命中危险正则：Gateway 返回 safety_blocked，本轮终止
→ 没命中危险正则：命令立即执行
```

File 与 Terminal 的策略分别位于两个工具内部，也没有一种统一状态告诉 AgentLoop“操作尚未失败，只是在等待用户决定”。

#### 1.3 解决办法

远程会话改为默认审批：File 的 `read`、`read_range`、`list`、`stat`、`write`、`append`、`replace` 全部先返回 `awaiting_approval`；Terminal 除严格识别的单条纯 `cd` 或 `pwd` 外全部先审批。File 的 `pwd/context` 只返回会话路径状态，不触达文件内容，因此保持可直接调用。

Terminal 的免审批规则采用窄白名单，并拒绝 Shell 控制符、变量展开和命令替换：

| 命令 | 处理方式 |
| --- | --- |
| `cd /d/work` | 直接执行 |
| `pwd` | 直接执行 |
| `cd /d/work && ls` | 等待审批 |
| `rm 1.md` | 等待审批 |
| `python script.py` | 等待审批 |

AgentLoop 识别第一个 `approval_required` 后返回独立的 `awaiting_approval` 状态，同一模型响应里的后续工具调用只生成 `approval_deferred`，不会继续分发。这样“等待决定”不会被误当成可修正错误，也不会让模型循环重试并制造多份请求。

### 2. 口头批准可能执行一条被重新生成的命令

#### 2.1 问题描述

如果机器人只问“是否允许删除文件”，用户回复“允许”后再让模型生成一次工具调用，第二次生成的路径、命令或写入内容可能已经改变。即使变化是模型无意造成的，用户批准的操作与真正执行的操作也不再一致。

另一个风险是把批准保存成会话级或规则级放行。一条 `rm` 获得批准后，同一会话的后续 `rm` 可能共享许可，审批就从“一次明确操作”扩大成“一类未来操作”。

#### 2.2 原因分析

审批对象必须是不可变的操作实例，而不是自然语言意图或危险规则名称。一次 File 写入至少由 action、path、content 和覆盖选项共同决定；一次 Terminal 执行则由完整 command 和当时的 backend 会话决定。

容易混淆的三种身份如下：

```text
审批编号     → 用户在飞书里选择哪一个请求
tool_call_id → 该请求对应哪一条模型工具调用
完整参数     → 获批后唯一允许执行的数据
```

只校验其中任意一个都不充分：编号可能被拿去配另一组参数，tool call 可能被模型重新生成，参数也不能脱离原 route、conversation 和请求者单独放行。

#### 2.3 解决办法

待审批 Tool Result 只携带请求编号和安全摘要，AgentLoop 从原始 tool call 绑定完整 arguments 与 `tool_call_id`。Gateway 展示 Terminal 命令及工作目录；File 展示解析后的路径、action，以及写入内容的字节数、摘要和脱敏预览。数据库保存的仍是完整原始参数，不依赖用户回复重建。

批准时同时校验以下不变量：

```text
route_key 相同
AND conversation_id 相同
AND requester_user_id 相同
AND request.status == pending
AND approval_grant.tool_name == request.tool_name
AND approval_grant.arguments == request.tool_args
```

状态通过比较并交换从 `pending` 进入 `executing`。工具侧也重新比较许可中的工具名和完整参数；只要路径、命令或内容发生变化，许可就失效并重新进入审批。执行结束后，请求进入 `executed` 或 `failed`，后续重复 `/approve` 只能读取终态，不能再次执行。

### 3. 等待用户回复会跨越消息和进程生命周期

#### 3.1 问题描述

远程审批不是一次函数调用内可以完成的同步交互。机器人发出问题后，用户可能几分钟后才回复，期间 Gateway 可能重启、同一路由可能收到其它消息，飞书也可能重复投递批准消息。

如果待审批状态只保存在内存，就会出现以下场景：

```text
操作尚未执行
→ Gateway 发出审批问题
→ 进程重启，内存请求丢失
→ 用户回复批准，但系统无法确认原始参数
```

更危险的窗口位于执行开始之后：命令可能已经产生副作用，但进程在保存结果前退出。此时自动重试可能让删除、追加或外部程序执行两次。

#### 3.2 原因分析

审批问题已经送达用户不等于请求已经安全持久化；请求进入 `executing` 也不等于系统知道命令是否完成。审批状态、Tool Result、用户可见问题和 Outbox 投递拥有不同生命周期，必须明确事务边界和恢复语义。

合法状态转换为：

| 当前状态 | 事件 | 下一状态 |
| --- | --- | --- |
| `pending` | 原请求者批准 | `executing` |
| `pending` | 拒绝、超时、`/stop`、`/new` | `denied/expired/cancelled` |
| `executing` | 工具返回 | `executed/failed` |
| `executing` | 进程重启 | `execution_unknown` |

`execution_unknown` 不能自动回到 `pending`，因为本机副作用无法统一提供幂等键或事务回滚。

#### 3.3 解决办法

审批请求使用 SQLite 持久化，并与原始 tool message 一一关联。创建请求、写入审批问题、建立消息投递关联和放入 Outbox 在同一事务中完成，因此不会出现“用户收到问题但数据库没有请求”的已提交状态。

等待期间，同一对话的普通消息只返回当前审批编号，不再启动模型；`/approve` 和 `/deny` 在 Gateway 路由层被直接处理，不交给模型解释。`/stop` 与 `/new` 会关闭尚未开始执行的请求，超时请求也会把原 Tool Result 改为明确的过期结果。

批准后的顺序固定为：

```text
pending --CAS--> executing
→ 使用数据库中的原始参数执行工具
→ 用真实结果替换 awaiting Tool Result
→ 请求进入 executed 或 failed
→ 以受信任的恢复消息继续原对话
```

启动恢复时，遗留的 `pending` 按截止时间收敛，遗留的 `executing` 一律转为 `execution_unknown`。这项取舍优先避免重复副作用；结果不确定时需要用户重新检查实际文件状态，再发起一条新的操作。

### 4. 子 Agent 的临时 backend 无法在批准后可靠恢复

#### 4.1 问题描述

Delegate 子 Agent 可以使用 File 和 Terminal，但它拥有独立的临时 `session_key` 和 backend。子任务返回时 backend 会被清理；如果子 Agent 在中途提出审批，用户稍后批准时，原 cwd 和会话环境已经不存在。

例如子 Agent 先执行 `cd /d/project-a`，再请求读取 `./report.md`。如果批准后改用主 Agent backend 或重新创建子 backend，`./report.md` 可能解析到另一个目录。表面上参数没有变化，实际访问目标却已经改变。

#### 4.2 原因分析

相对路径和 Shell 命令的完整语义不只存在于 arguments，还依赖 backend 的 cwd、环境 snapshot 和生命周期。直接把子 Agent 请求提升给父会话执行，会破坏“批准参数等于执行语义”的核心不变量。

生命周期差异可以表示为：

```text
主会话 backend：conversation_id 持续存在，可在下一条消息恢复
子会话 backend：child_session_key 只在 delegate 运行期间存在，结束即清理
```

要支持嵌套审批，必须额外持久化并恢复子 Agent 的完整执行环境，还要处理后台 delegate、取消和多个子任务并发。这已经超出一次工具批准的状态所有权。

#### 4.3 解决办法

远程模式下，Delegate 不允许携带 File 或 Terminal 工具集运行，返回明确提示，要求主 Agent 直接调用对应工具。主会话随后产生普通审批请求，路径和命令继续绑定到可恢复的 conversation backend。

前后对比如下：

```text
旧：父 Agent → Delegate → 子 backend 访问文件
新：父 Agent → 直接调用 File/Terminal → 用户审批 → 主 backend 执行
```

Skill-only 的子任务不受这一限制。这个边界牺牲了远程子 Agent 自动拆分文件任务的便利，但避免把一次批准错误地应用到另一个 cwd 或环境。未来如果需要嵌套审批，应先为子 backend 建立独立的持久生命周期和恢复协议，而不是复用主会话许可。

### 5. `/approve` 不能占住同一路由的消息消费协程

#### 5.1 问题描述

飞书 Adapter 对每个 route 使用串行 Inbox consumer。consumer 会等待当前 `handle_message` 返回后才读取下一条消息，因此 `/approve` 如果一直等待工具执行和 Agent resume，同一路由的 `/stop`、`/new` 即使已经到达 Inbox，也没有机会进入 Gateway。

这不是提前释放某个 admission lock 就能解决的问题。真正被占住的是 Adapter 对消息 handler 的 `await`：只要 `/approve` handler 没有返回，route consumer 就不会读取下一项。

#### 5.2 原因分析

旧审批分支把三个生命周期不同的动作放进了同一个入站调用栈：

```text
/approve 入站
→ pending --CAS--> executing
→ 等待 Terminal/File 实际执行
→ 等待结果和 resume task 落库
→ 等待恢复逻辑接回 Agent
→ handler 返回
```

Terminal 可能运行数分钟，File 也可能遇到慢磁盘或网络挂载。route admission lock 只控制 Runner 的短临界区；即便工具执行前释放它，外层飞书 consumer 仍在等待原 handler，所以控制消息仍然无法被消费。

#### 5.3 解决办法

`/approve` 现在只负责身份校验、数据库 CAS 和后台任务登记。CAS 返回 `claimed` 后，Gateway 以完整 approval ID 创建唯一的受管理任务，保存原始 `route_key`、`conversation_id`、`asyncio.Task` 和专用 `threading.Event`，随后持久化“审批已通过，操作开始执行”的控制回执并立即返回。

后台编排继续复用既有 `_execute_claimed_approval` 和工具注册表，不复制 File、Terminal 的执行逻辑。真实工具执行期间不持有 route admission lock；工具返回后才重新进入短临界区，判断取消信号、当前 conversation 和 runtime fencing，再选择只保存终态，或原子保存终态并创建 approval resume。

任务完成回调会读取 Task 结果或异常，以摘要形式记录异常类型，并幂等清理 approval ID 与 route/conversation 两组索引。数据库 CAS 仍是唯一执行入口，所以重复 `/approve` 只会读到 `executing`、`executed` 或 `failed`，不会登记第二个工具任务。

### 6. 审批后的 Terminal 必须复用原有进程组取消链路

#### 6.1 问题描述

正常 AgentLoop 调用 Terminal 时会传入 `cancel_checker`，但旧审批执行直接调用 `registry.dispatch()`，没有携带该检查器。结果是普通 Terminal 能响应 `/stop`，同一命令一旦经过远程审批，反而脱离了 backend 已有的进程组中断机制。

直接对审批的 `asyncio.Task` 调用 `cancel()` 也不能解决问题。工具通过 `asyncio.to_thread()` 运行，取消等待协程不会自动终止线程中的 Shell 子进程，更不能替代 Windows Job Object、`CTRL_BREAK_EVENT` 或 POSIX 进程组信号。

#### 6.2 原因分析

取消能力属于一次执行实例，不能复用普通 Agent generation，也不能只存在于 asyncio Task 上。需要保持的调用链是：

```text
Gateway /stop
→ 审批实例 cancel_event.set()
→ Terminal dispatch_context.cancel_checker
→ run_terminal
→ backend.execute(cancel_checker=...)
→ backend 软中断并在必要时强制结束进程树
```

File 没有相同的协作式中断接口。把 Terminal 的 checker 无差别传给 File 会制造“File 已被物理取消”的错误语义，也会把 Gateway 和具体工具实现重新耦合起来。

另一个竞态发生在工具已经返回、审批回执 worker 尚未结束时。如果后台任务过早从运行索引移除并把 resume 排入队列，随后到达的 `/stop` 可能只取消回执 worker，旧 resume 仍会在队列接力后启动。

#### 6.3 解决办法

每个已认领审批拥有独立的 `threading.Event`。只有 `tool_name == "terminal"` 时，`cancel_event.is_set` 才会进入 dispatch context；Terminal 和 backend 因而继续走原有轮询、软中断和强杀实现，Gateway 不发送操作系统信号，也不重新实现进程终止。

`/stop` 同时取消普通 Agent、关闭当前 conversation 的 pending approval，并对所有匹配的运行中审批设置事件。三者任一发生就回复 `(cancel requested)`。Terminal 返回的 `cancelled` 结构化结果照常替换占位 Tool Result，审批收敛为现有 `failed` 终态，但不创建 resume，也不回到 `pending`。

后台审批在准备 resume 时保持运行登记，先等待当前 route worker 完成；等待期间不持有 route lock，`/stop` 和 `/new` 可以优先进入。对 File，事件不能撤销已经开始的文件操作，实际语义是“允许 File 自行完成并保存真实结果，但禁止后续 Agent resume”。Gateway shutdown 同样先设置所有审批事件，再等待任务完成必要收尾。

`/new` 会在切换前设置旧 conversation 的审批事件。工具返回后还会在 route 临界区重新比较保存的 conversation ID 与当前值；不相等时只把结果写回原审批和原 Tool Result，不把旧 resume、busy、generation 或 worker 状态接入新 conversation。

### 7. 审批授权不能在用户身份缺失时退化为 route 授权

#### 7.1 问题描述

审批创建、批准和拒绝过去都直接使用 `user_id or user_id_alt`。当两个字段都为空时，数据库可以保存空 requester；批准与拒绝逻辑又只在 stored requester 非空时比较身份，于是一次本应绑定用户的授权可能退化为“同 route 上任何人都能处理”。

route、chat、thread 和 conversation 只能说明消息来自哪里，不能证明是谁作出了批准。昵称和显示名称也可能变化或重复，都不能补齐稳定用户身份。

#### 7.2 原因分析

旧条件的缺陷在于把空值当成“无需比较”：

```text
if stored_requester_user_id:
    compare_identity()
```

这使身份校验成为可选项。历史数据库中的空 requester 也可能在后续 `/approve` 或 `/deny` 时被当前操作者事实认领，破坏审批创建时已经确定的授权边界。

身份缺失还可能让原 Agent 永久停留在 `awaiting_approval`：系统既不能安全创建 pending 请求，又没有把已写入的占位 Tool Result 收敛为明确失败。

#### 7.3 解决办法

Gateway 使用单一 `_stable_actor_id` 解析方法，只按平台稳定 `user_id`、其次 `user_id_alt` 取值，并把空字符串和纯空白视为不可用；不使用昵称、route、chat、conversation 或 message ID 回退。

创建审批前若 actor ID 缺失，Gateway 不调用正常审批创建事务，而是在独立数据库事务中把已经绑定 request ID 的占位 Tool Result 替换为 `approval_identity_unavailable`，随后让 Agent worker 以明确用户提示正常收敛。数据库创建入口也强制 requester 为非空字符串，并把 requester 纳入审批幂等身份比较，防止其它调用点绕过 Runner。

`/approve` 和 `/deny` 在 Runner 入口缺少 actor ID 时立即拒绝；数据库边界同时要求 current actor 与 stored requester 都非空且完全一致，之后才继续 conversation、状态和 CAS 校验。已有空 requester 的历史审批因此只能得到 `forbidden`，不会被批准、拒绝认领或补写身份，也不需要数据库迁移。
