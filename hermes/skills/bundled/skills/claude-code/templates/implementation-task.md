在以下工作目录完成实现任务：
<目标工作目录>

任务目标：
<目标>

验收标准：
<验收标准>

允许修改：
<允许修改的文件或目录>

禁止修改：
<禁止修改的文件或目录>

新增文件权限：
<是否允许新增文件；允许时写明范围>

测试权限：
<是否允许运行测试；允许时写明范围；禁止时明确写“不要修改测试，不要运行测试”>

依赖修改权限：
<是否允许修改依赖、manifest 或 lockfile>

Git 权限：
<是否允许创建 commit；始终禁止未授权的 push>

网络权限：
<是否允许访问网络及允许范围>

重构边界：
<是否允许扩大重构范围；默认只做完成目标所需的最小改动>

阶段要求：
代码修改阶段与测试阶段严格分离。先完成获准的代码修改并总结；只有测试被明确允许时，才进入独立测试阶段。不要把未授权的测试或修复混入当前阶段。

安全要求：
不要请求、读取或输出密码、token、API key 或其他凭证。不要绕过权限或安全检查。遇到范围不明、高风险、工作区外路径、发布、部署或 Git push 时停止并说明。

完成后按以下格式汇报：
<汇报格式：已完成内容、修改文件、执行的命令或检查、约束遵守情况、未完成事项和风险>

完成指定内容后停止，不要开始范围外工作。
