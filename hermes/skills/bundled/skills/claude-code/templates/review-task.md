在以下工作目录执行审查任务：
<目标工作目录>

审查目标：
<审查对象与问题>

审查范围：
<允许读取和分析的文件、diff 或提交>

禁止范围：
<禁止访问、修改或扩大分析的内容>

修改权限：
<默认只审查不修改；如允许修改，明确列出文件和边界>

新增文件权限：
<是否允许新增文件；默认不允许>

测试权限：
<是否允许运行测试；允许时写明范围；禁止时明确写“不要修改测试，不要运行测试”>

依赖、Git 与网络权限：
<是否允许修改依赖、创建 commit 或访问网络；未明确授权的操作不要执行，禁止未授权的 push>

重构边界：
<是否允许提出或执行扩大范围的重构；默认只报告与审查目标直接相关的问题>

阶段要求：
代码修改阶段与测试阶段严格分离。审查阶段不要隐式进入修复或测试阶段；需要额外操作时先说明并等待指示。

安全要求：
不要请求、读取或输出密码、token、API key 或其他凭证。不要绕过权限或安全检查。遇到范围不明、高风险、工作区外路径、发布、部署或 Git push 时停止并说明。

按以下格式汇报：
<汇报格式：按严重程度列出发现、证据位置、影响、建议、未验证事项；没有发现时明确说明残余风险>

完成审查后停止，不要修改未获准内容。
