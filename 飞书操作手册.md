# 飞书操作说明

MyHermes 的飞书入口既支持普通自然语言对话，也支持少量 `/xxx` 会话控制命令。文件、Terminal、Cron 等能力由 Agent 工具完成，不需要为每个工具记忆一条斜杠命令。

实际可用能力取决于 `config.yaml` 中飞书平台的 `toolsets`、文件传输开关、用户或群聊白名单以及审批策略。

## 快速开始

启动统一 Gateway：

```powershell
python main.py --gateway
```

在飞书中向机器人发送普通文本即可开始对话，例如：

```text
读取 D:\my-hermes\README.md，概括这个项目当前支持的能力。
```

群聊是否需要 `@机器人`、是否只允许私聊，以及哪些用户或群可以使用，由飞书平台配置决定。

## 飞书聊天命令

### `/new`

创建一个全新的对话，后续消息不再使用当前对话历史。

```text
/new
```

如果当前任务仍在运行，系统会请求取消当前任务、清理旧的待处理消息和待审批操作，然后切换到新对话。历史记录仍保存在数据库中，可以稍后通过 `/sessions` 和 `/resume` 找回。

`/new` 不接受参数。

### `/stop`

请求停止当前正在运行的 Agent、工具或审批操作。

```text
/stop
```

可能返回：

- `(cancel requested)`：已经发出取消请求；
- `(no active task)`：当前没有可取消的任务。

这是协作式取消。已经完成的外部副作用不会被自动撤销，正在执行的操作会在其支持的取消边界停止。

`/stop` 不会删除对话历史，也不接受参数。

### `/status`

查看当前对话的运行状态。

```text
/status
```

状态中包含当前对话 ID、是否忙碌、是否已请求取消、排队消息数量和空闲时间等信息。

`/status` 不接受参数。

### `/sessions`

列出当前飞书路由最近的对话，最多显示 10 条。

```text
/sessions
```

列表会显示序号、当前对话标记、短对话 ID、消息数量、最近内容预览和活跃时间。序号只对应本次显示的列表。

`/sessions` 不接受参数。

### `/resume`

切换回一个已有对话，可使用 `/sessions` 返回的序号或完整 `conversation_id`。

```text
/resume 2
```

或：

```text
/resume <完整 conversation_id>
```

注意：

- 不带参数时只会返回用法提示；
- 当前任务仍在处理时不能切换，应等待完成或先发送 `/stop`；
- 使用序号前应重新执行 `/sessions`，避免列表变化后选错对话；
- 输入 ID 时必须使用完整 `conversation_id`，不是列表中的短 ID。

### `/approve`

批准当前对话中的待审批操作。审批消息会展示工具、风险、路径、命令或 Cron capability scope 等信息。

单次批准当前唯一的待审批请求：

```text
/approve
```

显式指定单次授权：

```text
/approve once
```

批准并在当前会话保留受限授权：

```text
/approve session
```

指定审批编号：

```text
/approve <审批编号>
/approve <审批编号> once
/approve <审批编号> session
```

注意：

- 默认审批有效期为 600 秒，可通过配置调整；
- 只能由发起原任务的用户处理；
- `session` 不是无限授权，只匹配当前会话、结构化参数规则和风险上限；
- 并非所有风险等级都允许 session 授权，应以审批消息显示的选项为准；
- 参数、任务或 capability scope 改变后，旧批准不能复用；
- 审批执行期间再次发送普通任务不会并行执行批准操作。

### `/deny`

拒绝待审批操作，工具不会执行。

拒绝当前唯一的待审批请求：

```text
/deny
```

指定审批编号：

```text
/deny <审批编号>
```

审批过期、已执行、已取消或不属于当前用户时，系统会返回对应状态，不会重复执行工具。

## 不存在的聊天命令

当前没有以下飞书命令：

- `/help`
- `/cron`
- `/cancel`
- `/clear`
- `/reset`

不要使用 `/resume` 恢复 CronJob；它只用于切换普通对话。Cron 的创建、暂停、恢复、手动运行和历史查询都应使用自然语言。

例如：

```text
列出我的所有定时任务。
```

```text
暂停任务 7ad...。
```

```text
恢复任务 7ad...，不要补跑暂停期间的历史执行。
```

## 飞书中可以使用的能力

### 普通 Agent 对话

- 连续多轮对话；
- 自动加载项目上下文、记忆和允许读取的 Skill；
- 长对话压缩、模型重试和 fallback；
- 连续发送的短文本可合并为一轮任务；
- 当前任务繁忙时，后续消息进入有界队列；
- Gateway 重启后恢复已持久化的消息和待发送回复。

### Terminal

当飞书平台启用 `terminal` toolset 时，可以让 Agent 执行命令，例如：

```text
在项目目录运行 git status，并解释当前状态，不要修改文件。
```

危险命令、敏感路径或高风险行为可能触发 `/approve`。Local Terminal 的路径策略属于应用层控制，不是操作系统级强沙箱。

### 文件操作

当启用 `file` toolset 时，可以读取、分段读取、列目录、查看文件信息、写入、追加和替换文件。

```text
读取 README.md，检查运行命令是否与 main.py 一致。
```

写入、敏感文件和越界路径可能被拒绝或要求审批。Agent 不能通过模型参数覆盖 Gateway 的可信身份或文件策略。

### 长期记忆

当启用 `memory` toolset 时，可以让 Agent 查看或更新长期记忆和用户档案。

```text
记住：我希望默认使用中文回复，代码标识符保持英文。
```

### Skill

当启用 `skill_read` 时，可以列出和读取本地 Skill：

```text
列出当前可用的 Skills，并简要说明各自用途。
```

当前示例飞书配置只启用 `skill_read`，没有启用 `skill_manage`，因此默认不能从飞书创建、修改或删除 Skill。

### Delegate 子任务

当启用 `delegate` toolset 时，Agent 可以把独立子问题交给隔离的子 Agent，并查询、获取或取消后台任务。

```text
把“检查 Cron 模块的数据流”委派给子 Agent，主会话同时总结 Gateway 的数据流，最后合并结果。
```

Delegate 不会自动继承主会话的全部权限，也不能无限递归创建子 Agent。

### Cron 定时任务

当启用 `cron` toolset 时，可以用自然语言执行：

- `create`：创建任务；
- `list`：列出任务；
- `get`：查看单个任务；
- `update`：修改任务；
- `pause`：暂停任务；
- `resume`：恢复任务；
- `run`：手动创建一次运行；
- `delete`：软删除任务定义；
- `history`：查看脱敏运行历史。

创建任务时应明确最小工具范围、时间和 timezone，例如：

```text
创建一个定时任务：每天 09:00，Asia/Shanghai，读取
D:\my-hermes\logs 里的最新日志并在当前飞书会话回复摘要。
只需要 file，不写文件，不需要 terminal 和 delegate，最长执行 120 秒。
```

需要生成并发送文件时，应明确说明：

```text
每周一 09:00 生成上周项目摘要，允许写入本次运行的产物目录，
完成后向当前飞书会话发送文本摘要和产物文件。
```

创建高风险任务或扩大权限时会出现 Cron 专用审批。审批通过后，任务仍只能使用持久 grant 中明确列出的工具、目录、Terminal executable、风险上限和投递目标。

### 发送本地文件

当启用 `messaging` toolset 和文件传输后，可以要求 Agent 把本地文件发送到当前飞书会话：

```text
把 D:\my-hermes\README.md 发送给我。
```

每次发送都会重新检查路径、敏感文件规则、普通文件类型、大小和稳定快照，并触发单次 `/approve`。批准后由持久 Delivery 上传并发送；进程重启不会要求 Agent 重新生成文件。

## 向 Agent 发送飞书附件

支持的入站消息类型：

- 文本；
- 图片；
- 普通文件；
- 音频；
- 媒体文件。

文件传输必须在配置中启用。推荐操作顺序：

1. 先发送一个或多个附件；
2. 等待机器人回复“文件已接受。”；
3. 再发送具体任务，例如“总结刚才的文件”或“比较刚才两个文件”。

附件会下载到受控缓存，并在随后发送的文字指令中绑定给 Agent。默认示例配置的单个入站、出站文件上限都是 20 MiB，实际限制以当前配置为准。

不要通过附件发送 API Key、access token、`.env` 或其他凭据。敏感文件即使位于 allowed roots 中，也可能被拒绝读取或发送。

## 群聊和私聊边界

飞书 Adapter 支持以下访问控制：

- 只允许私聊；
- 群聊必须 `@机器人`；
- 用户白名单；
- 群聊白名单；
- 用户和群聊白名单采用 `and` 或 `or` 组合；
- 忽略机器人自身发送的消息。

同一个用户在不同群聊、话题或私聊中使用不同的 route key，不会默认共享普通对话运行状态。

## HTTP 运维端点不是聊天命令

以下路径由飞书 Webhook HTTP 服务使用，不能在飞书聊天中当作命令发送：

| HTTP 路径 | 作用 |
|---|---|
| `/feishu/webhook` | 默认飞书事件回调地址，可在配置中修改 |
| `/livez` | 检查进程是否存活 |
| `/readyz` | 检查 Gateway 是否已获得 runtime lease 并可处理请求 |

`/quit` 和 `/exit` 只属于本地 CLI Adapter，不是飞书命令。

## 常见使用建议

- 想保留上下文时直接继续发送消息；想完全换一个话题时使用 `/new`。
- 任务没有响应时先使用 `/status`，确认繁忙后再决定是否 `/stop`。
- 审批前认真检查命令、路径、工具范围和投递目标，不要机械批准。
- 创建 Cron 时主动说明“不需要 terminal”“只读文件”等最小权限要求。
- 发送附件后补充明确的文字任务，不要只发送文件后等待 Agent 猜测意图。
- 需要回到旧对话时先 `/sessions`，再用最新序号执行 `/resume`。
